
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Launch</title>


 <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Orbit">
  <style>
body {
      font-family: Orbit;
      text-align: center;
      color: white;
  background-repeat: no-repeat;
  background-attachment: fixed; 
  background-size: 100% 100%;
      background: black;
    }
    .gallery {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      margin-top: 2em;
      justify-content: flex-start;
    }
    .card {
      border: 1px solid white;
      font-family: orbit;
      border-radius: 10px;
      margin:auto; 
      display:block;
      color: black;
      padding: 20px;
      width: 320px;
      text-align: center;
      background-color: #ffffff;
      box-shadow: 0 5px 10px rgba(0, 0, 0, 0.1);
      opacity: 0.5;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

  </style>
</head>

<body>

<h1> Quasar: Lux Launcher </h1>

<button class="card button" onclick="launchGame()">Launch</button>

<script>
async function launchGame() {

   const url = "https://raw.githubusercontent.com/arcimiedies/Quasar/refs/heads/main/index.html";

   try {
      const res = await fetch(url);
      let html = await res.text();

      const base = url.slice(0, url.lastIndexOf("/") + 1);

      if (/<head/i.test(html)) {
         html = html.replace(
            /<head([^>]*)>/i,
            `<head$1><base href="${base}">`
         );
      } else {
         html = `<head><base href="${base}"></head>` + html;
      }

      const w = window.open("about:blank");
      w.document.write(html);
      w.document.close();

   } catch (err) {
      alert("Failed to load game.");
      console.error(err);
   }
}
</script>

</body>
</html>
